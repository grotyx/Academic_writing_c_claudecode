# Python dependencies for the manuscript verification harness (scripts/, tests/).
#
# DOCX compilation and external multi-model review need third-party packages.
# Core gate scripts (check_citations.py, check_gate.py, check_numbers.py,
# check_revision_claims.py, lint_manuscript.py, search_pubmed.py) are
# standard-library only.
#
# Install with:  pip install -r requirements.txt

python-docx>=1.1.0   # DOCX compilation in scripts/compile_response_docx.py
requests>=2.31.0     # OpenRouter calls in scripts/critical_review.py

# Development / test runner (optional but recommended):
# pytest>=8.0
